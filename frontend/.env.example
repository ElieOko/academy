# URL de l’API FastAPI (sans slash final).
# En local, laisser vide : Vite proxifie /api vers http://127.0.0.1:8765.
# En production, le build utilise automatiquement https://backend.acad-emy.com/api.
VITE_API_URL=
